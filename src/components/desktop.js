const Window = require("./window");

module.exports = React.createClass({
    render: () => (
        <div>
            <Window title="Untitled" />
            <Window title="Untitled 2" />
        </div>
    )
});
